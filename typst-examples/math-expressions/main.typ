= Mathematical Formulas

== Inline Math

The quadratic formula is $x = (-b plus.minus sqrt(b^2 - 4a c)) / (2a)$.

The famous equation by Einstein: $E = m c^2$.

== Display Math

The integral of a function:

$ integral_a^b f(x) dif x = F(b) - F(a) $

The Pythagorean theorem:

$ a^2 + b^2 = c^2 $

== Matrices

$ mat(
  1, 2, 3;
  4, 5, 6;
  7, 8, 9
) $

== Calculus

The derivative of $f(x) = x^2$ is:

$ (dif f)/(dif x) = 2x $

The limit:

$ lim_(x -> infinity) 1/x = 0 $