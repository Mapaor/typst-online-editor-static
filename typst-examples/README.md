## How this works?

Each folder contains one (1) example consisting of Typst documents. For convention the compiled main file is named main.typ.

Then these files (code) are fetched into the examples in the \src\lib\typst\examples\TypstExamples.ts typescript file.